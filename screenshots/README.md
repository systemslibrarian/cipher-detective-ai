# Screenshots

Drop UI screenshots and short demo GIFs here. Recommended:

- `detect-mode.png` — the Detect tab analyzing a Caesar cipher.
- `explain-mode.png` — the Evidence Notebook view.
- `challenge-mode.png` — a generated challenge.
- `compare-mode.png` — heuristic vs Transformer side-by-side.
- `hero.png` — a wide hero image used at the top of the README.

Keep file sizes modest (under ~500 KB each) so the README loads quickly.
